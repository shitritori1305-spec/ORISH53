Expo mobile scaffold (minimal)

Instructions:

- Install Expo CLI: `npm install -g expo-cli` (or `pnpm`/`yarn` equivalent).
- From `mobile/` run `npm install` then `expo start`.

This folder contains a minimal `App.js` that calls the same API at `/questionnaire`.
