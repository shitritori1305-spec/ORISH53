React frontend scaffold (Vite)

This folder is a placeholder for a future React/Vite migration. To create a full React app run:

```bash
cd web-react
npm init vite@latest my-app -- --template react
cd my-app
npm install
npm run dev
```

Then point the UI to `http://localhost:3000` API endpoints.
